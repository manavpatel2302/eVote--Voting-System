# Production Environment Variables for Vercel
# Copy these to your Vercel dashboard under Settings > Environment Variables

NODE_ENV=production
PORT=3000

# MongoDB Atlas Connection String
# Replace with your MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority

# JWT Secret (MUST be different from development)
JWT_SECRET=your-production-jwt-secret-key-change-this-to-something-secure-and-long

# Frontend URL (will be your Vercel deployment URL)
FRONTEND_URL=https://your-app-name.vercel.app

# Email Configuration (Optional - for production email features)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-production-email@gmail.com
# EMAIL_PASS=your-production-app-password